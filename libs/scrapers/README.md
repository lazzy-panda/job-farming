# scrapers

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build scrapers` to build the library.

## Running unit tests

Run `nx test scrapers` to execute the unit tests via [Jest](https://jestjs.io).
